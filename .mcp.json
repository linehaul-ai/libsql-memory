{
  "mcpServers": {
    "fff-memory": {
      "command": "cargo",
      "args": [
        "run",
        "--quiet",
        "--manifest-path",
        "${CLAUDE_PLUGIN_ROOT}/rust/Cargo.toml",
        "--bin",
        "fff-memory",
        "--",
        "serve",
        "--project",
        "${CLAUDE_PROJECT_DIR}"
      ]
    }
  }
}
