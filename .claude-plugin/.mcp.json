{
  "mcpServers": {
    "libsql-memory": {
      "command": "${CLAUDE_PLUGIN_ROOT}/../src/plugin/build/libsql-memory",
      "args": [
        "--database-path", ".claude/memory.db",
        "--embedding-provider", "nomic",
        "--embedding-endpoint", "http://192.168.128.10:1234/v1/embeddings"
      ]
    }
  }
}
